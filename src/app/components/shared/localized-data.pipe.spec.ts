import { LocalizedDataPipe } from './LocalizedDatePipe';

describe('LocalizedDataPipe', () => {
  /*it('create an instance', () => {
    const pipe = new LocalizedDataPipe();
    expect(pipe).toBeTruthy();
  });*/
});
